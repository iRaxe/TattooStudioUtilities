# Database Configuration per sviluppo locale
# Usa SQLite per sviluppo locale
USE_SQLITE=true
DATABASE_URL=sqlite:./tinkstudio_local.db

# PostgreSQL config (non usato in locale)
PGHOST=localhost
PGPORT=5432
PGDATABASE=tinkstudio
PGUSER=postgres
PGPASSWORD=tinkstudio_secure_password
PGSSL=false

# JWT Secret
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Admin Credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# Gift Card Configuration
GIFT_CARD_VALIDITY_MONTHS=4

# Base URL per sviluppo locale
PUBLIC_BASE_URL=http://localhost:3000